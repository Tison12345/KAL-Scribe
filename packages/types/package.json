{
  "name": "@kal-scribe/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types for the clinical-ai module (mirrored into python/asr-service's Pydantic models — see architecture.md §16).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
