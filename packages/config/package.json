{
  "name": "@kal-scribe/config",
  "version": "0.0.0",
  "private": true,
  "description": "Typed env parsing, one function per app (mirrors Repo B's @cms/config parseApiEnv pattern — see architecture.md §4).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  }
}
