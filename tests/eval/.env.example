# Copy to .env. Same LLM provider config as workers/clinical-ai-worker
# (architecture.md §10) — the eval harness calls the same provider
# abstraction, not a separate code path.
LLM_PROVIDER=groq
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
